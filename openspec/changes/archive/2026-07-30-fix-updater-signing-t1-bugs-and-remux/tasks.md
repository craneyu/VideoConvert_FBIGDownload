## 1. 簽章金鑰輪替（含人工前置）

- [x] 1.1 依決策「輪替簽章金鑰並改填公鑰」產生全新 minisign 金鑰對，並把私鑰與其 passphrase 寫入 GitHub Secrets（`TAURI_SIGNING_PRIVATE_KEY` 與 `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`）。**此步驟必須由人工執行，實作代理無法完成，且阻擋 1.2 與第 7 組的驗證。** 完成後發佈流程能以新私鑰簽章。驗證：Secrets 已存在且發佈流程執行時不再出現簽章金鑰缺失錯誤。
- [x] 1.2 更新設定中的驗簽金鑰欄位改填新公鑰檔內容，滿足 `Secure Update Installation`：欄位解碼後為 minisign 公鑰、不含 KDF 欄位、註解不含 secret key 字樣。驗證：以一行 base64 解碼指令檢視該欄位輸出，確認不含 secret key 字樣；並對整個 repo 全文搜尋新私鑰字串，結果必須為零命中。
- [x] 1.3 新增 `docs/updater-key-rotation.md`，記錄金鑰產生、Secrets 設定、公鑰欄位填寫方式，以及「私鑰不得以任何形式進入版控」的禁止事項，使此流程可被重複執行。驗證：內容審查 —— 上述四項皆有對應段落，且文件本身不含任何私鑰內容。

## 2. 更新管線可實際運作

- [x] 2.1 依決策「更新 manifest 檔名與 endpoint 對齊」，把 updater endpoint 由 repo raw 路徑改為指向 release 資產，並讓發佈流程產出正式 release（非 draft、非 pre-release），滿足 `Automatic Update Check` 中「endpoint 解析到發佈流程實際產生的 manifest」與「檔名一致」兩項。驗證：內容審查確認 endpoint 所請求的檔名與發佈流程產出的 manifest 檔名字串完全相同，且發佈設定不再標記為 draft 或 pre-release。
- [x] 2.2 [P] 依決策「更新檢查失敗改為寫入 log」，讓更新檢查的失敗路徑寫入錯誤 log 而非落入空的 catch，且不彈出對話框、不阻擋啟動。驗證：暫時把 endpoint 指向不存在的位址啟動應用程式，確認 log 出現失敗原因、應用程式仍正常啟動且無對話框。
- [x] 2.3 [P] 依決策「release workflow 改由版本 tag 觸發」，把發佈觸發條件由推送 main 改為推送版本 tag 並加入手動觸發，消除每次推送重跑三平台建置並重複上傳到既有 release 的浪費。驗證：推送一個不含 tag 的 commit 到 main 後確認未觸發發佈流程；推送版本 tag 後確認觸發。

## 3. 前端事件監聽生命週期

- [x] 3.1 依決策「事件監聽改用 effect 管理生命週期」，把下載進度、轉檔進度、檔案拖放三個監聽改為可正確清理（註冊一次、檢視銷毀時移除），滿足 `Batch File Selection` 中「往返不增加監聽數」與「單次拖放每檔只建一筆任務」。不需 cleanup 的初始化邏輯留在 `onMount`。驗證：`npm run check` 的錯誤數由 2 降為 0（其中包含 async `onMount` 的型別錯誤）；並實機在主畫面與設定頁往返兩次後拖入 1 個影片檔，確認轉檔清單只增加 1 筆。

## 4. 下載歷史正確性

- [x] 4.1 [P] 依決策「下載失敗時寫回歷史紀錄狀態」，讓下載失敗時把對應紀錄狀態寫為失敗，並在紀錄尚未建立就失敗時只回報錯誤而不嘗試更新，滿足 `SQLite Persistence` 的兩個失敗情境。驗證：以無效網址觸發下載失敗，切到下載歷史頁確認該筆顯示為失敗而非停留在下載中；另以取得影片資訊階段就失敗的網址確認不產生資料庫錯誤。
- [x] 4.2 [P] 依決策「以新增 migration 校正時間戳為本地時間」新增 migration，將建立時間預設改為本地時間並把既有 UTC 值轉為本地時間（以系統時區推導，不寫死位移），既有 migration 不得修改，且先複製資料再移除舊表，滿足 `Local Time Timestamp Migration`。預設值變更與既有資料回填必須在同一個 migration 版本內完成，不得拆成兩個版本（否則會產生無法分辨時區的混合資料）。驗證：以含既有紀錄的資料庫升級後，比對紀錄總數不變、既有時間戳位移正確；**並且同時檢查一筆 migration 前既有的紀錄與一筆 migration 後新建立的紀錄，兩者在歷史頁顯示的日期都正確**（其中至少一筆需為本地時間 00:00 至 08:00 之間建立）；重啟應用程式確認 migration 不重複執行、時間不再次位移。

## 5. 跨平台與轉檔相容性

- [x] 5.1 [P] 依決策「開啟檔案位置改為跨平台實作」，讓開啟檔案位置在 macOS、Windows、Linux 皆實際開啟檔案管理器，並在無法執行時回傳錯誤而非回報成功，滿足 `Local File Access`。驗證：在 macOS 與 Windows 各手動點選一次確認檔案被選中；並確認不支援情形回傳錯誤字串而非成功。
- [x] 5.2 [P] 依決策「H.265 輸出加上 hvc1 容器 tag」，讓 H.265 輸出至 MP4 時帶上 `hvc1` 視訊 codec tag，並在轉檔命令中明確指名音訊編碼器，滿足 `Format Conversion`。驗證：轉出一支 H.265 MP4，以 `ffprobe` 確認 codec tag 為 `hvc1`，並在 macOS QuickTime 實際播放成功。

## 6. 下載錯誤傳達與條件式 remux

- [x] 6.1 依決策「讀取並回傳 yt-dlp 的 stderr」，改為持續讀取所有被導向管線的輸出串流（以固定容量保留最後若干行），使管線塞滿不再阻塞子行程，且失敗時回傳的錯誤包含來源工具的實際輸出而非固定字串，滿足 `Progressive Downloading`。驗證：以私人或已失效影片網址觸發下載失敗，確認錯誤訊息含 yt-dlp 自身描述的原因；並確認錯誤輸出量大時下載仍能執行到結束而不卡住。
- [x] 6.2 依決策「以 ffprobe 判斷後決定 remux 或重新編碼」，先以測試驅動方式實作相容性判斷的純函式：輸入為探測到的視訊編碼、音訊編碼與畫面尺寸，輸出為 remux 或重新編碼的決策，涵蓋 `Post-Download Container Optimization` 所定義的白名單。驗證：`cargo test` 新增測試覆蓋規格決策表全部 7 個案例（h264+aac 偶數尺寸為 remux、無音訊軌為 remux、音訊非 AAC 為重新編碼、視訊非 H.264 為重新編碼、寬為奇數為重新編碼、高為奇數為重新編碼、探測輸出無法解析為重新編碼），全部通過。
- [x] 6.3 把 6.2 的判斷接入下載完成流程：以 `ffprobe` 探測後分流至串流複製搭配 faststart 的容器 remux 或既有重新編碼路徑（重新編碼路徑保留不刪），並讓下載進度事件的狀態文字反映實際走的分支，滿足 `Post-Download Container Optimization` 的三個情境。驗證：下載一支來源為 H.264 加 AAC 且尺寸為偶數的影片，確認狀態文字顯示容器最佳化、該階段耗時明顯短於重新編碼、輸出檔可播放；再下載一支非 H.264 來源，確認走重新編碼且輸出可播放；並確認探測失敗時退回重新編碼而非讓下載失敗。

## 7. 發佈與端到端驗收

- [x] 7.1 提升版本號並推送版本 tag，產出帶簽章的正式 release 與更新 manifest。驗證：以 HTTP 請求取得 updater endpoint 所指向的 manifest 回應成功，且其中的版本號與本次發佈版本一致、簽章欄位非空。
- [x] 7.2 從修復前的版本觸發自動更新並完成安裝，確認驗簽通過（此為 `Automatic Update Check` 與 `Secure Update Installation` 的端到端驗收）；同時在發佈說明中載明既有 0.1.3 使用者需手動下載一次，之後自動更新才會接上。驗證：實機從舊版本完成一次升級並確認版本號變更；發佈說明內容審查確認已含該告知。
